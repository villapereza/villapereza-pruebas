# Villa Pereza · Gestión de pruebas v7.1

- PIN de exactamente 4 cifras.
- PIN inicial por defecto: `1234`.
- La web muestra las versiones Web/API en la pantalla de acceso.
- Se eliminan los Service Workers y cachés antiguos para evitar mezclar versiones.
- La gestión de pruebas por usuario funciona con `adminSetUserAssignments` y tiene compatibilidad con backends anteriores usando `adminSetAssignments`.

Tras subir los archivos, abre `actualizar.html` una vez.


API configurada: nuevo despliegue v8 solicitado el 3 de agosto de 2026.
